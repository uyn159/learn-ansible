# learn-ansible

```

my-ansible-project/
├── playbook.yml       # Your main playbook
├── inventory          # Your inventory file (INI format in this case)
└── ansible.cfg        # Ansible configuration file (optional, but recommended)
```